# AI 활용 내용 — 신고표 초안

> 참가신청서에 들어가는 표의 **초안**입니다. 최종 기입은 현찬이 합니다.
> 요강 근거: *"AI 활용 내용을 신청서에 명시적으로 신고해야 함(모델·도구명 + 반영 정도). **숨기지 말 것.**"*
> 반영 정도는 **참고 / 일부반영 / 대부분활용** 셋 중 하나를 고릅니다.

숨겨서 얻을 것이 없습니다. 요강이 신고를 의무로 두었으므로 **정직한 기재가 곧 요건 충족**이고,
반대로 축소해서 적었다가 코드·문서에서 드러나면 그때는 요건 위반이 됩니다.

---

## 1. 표에 그대로 옮길 내용

| 항목 | 사용 AI 모델·도구 | 반영 정도 | 무엇에 썼는지 (필요 시 부기) |
|---|---|---|---|
| 아이디어 발굴 | Claude (Anthropic) | **일부반영** | 주제는 팀원 본인의 실제 경험(해외결제 실패·안심차단)에서 나왔습니다. AI는 역대 수상작과의 중복 검증, 아이디어 좁히기에 썼습니다 |
| 정보수집 | Claude (Anthropic) | **일부반영** | KB 공개문서 수집·정리에 활용했습니다. 다만 **1차 근거의 실재 확인은 사람이 원문에서 직접** 했습니다 |
| 데이터 분석 | **OpenAI gpt-4o-mini** (제품 내부) · Claude | **대부분활용** | 약관·FAQ 문서에서 조건을 추출하는 파이프라인 자체가 LLM 기반입니다. 추출 결과는 사람이 검수했습니다(16건 중 12건 채택) |
| 코드작성·디버깅 | Claude (Anthropic) | **대부분활용** | 파이프라인·판정 엔진·API·화면 구현. 설계 판단과 검증 기준은 팀이 정했습니다 |
| 문서 작성 | Claude (Anthropic) | **대부분활용** | 기획안·기술설명서 초안 작성 및 정합성 점검 |
| 이미지 제작 | **해당 없음** (이미지 생성 AI 미사용) | — | 외부 이미지를 한 장도 쓰지 않았습니다. 로고·아이콘은 **SVG·CSS 코드로 자작**했고, 그 코드 작성에 Claude를 썼습니다 |
| 발표자료 제작 | Claude (Anthropic) | **대부분활용** | 기술설명서 슬라이드 및 마크다운 → 슬라이드 변환 도구 |

---

## 2. 제품 **안에서** 쓰는 LLM (이건 따로 밝히는 편이 안전합니다)

위 표는 "만드는 과정에서 무엇을 썼나"를 묻습니다. 저희는 **결과물 안에서도** LLM을 쓰므로
기술설명서에 이미 적었고, 신청서 과제 요약에도 한 줄 넣는 것을 권합니다.

| 어디에 | 모델 | 쓰는 이유 |
|---|---|---|
| 조건 추출 (문서 → 조건 트리) | `gpt-4o-mini` | 비정형 문장을 `subject op value` 형태로 형식화 |
| **판정** | **쓰지 않음** | 실행할 때마다 답이 달라지면 안 되므로 규칙 엔진이 합니다 |

- 코드에 모델명이 박혀 있지 않습니다. `EXTRACT_MODEL` 환경변수로 주입합니다
  (`src/extract/extractor.py:33`). 조건 트리 파일에 `llm:gpt-4o-mini+merge-1.0+review`로
  **어느 모델이 만든 자산인지 기록**됩니다.
- API 키는 저장소에 없습니다. `.env`는 커밋되지 않고 pre-commit 훅이 한 번 더 막습니다.

---

## 3. 기재할 때 주의

- **"AI를 적게 썼다"고 적을 이유가 없습니다.** 평가 기준 100점 어디에도 AI 사용량 항목이
  없고, 「기술 적정성 20점」은 *많이 썼나*가 아니라 *맞는 곳에 썼나*를 봅니다.
  판정에 LLM을 쓰지 않은 것은 감점 요소가 아니라 **설명해야 할 설계 판단**입니다.
- 표에 적는 도구명은 **실제 쓴 것만** 씁니다. 안 쓴 도구를 채워 넣지 않습니다.
- 서약서 조항과 연결됩니다 — *"생성형 AI 등 외부 도구의 이용약관·라이선스·저작권 정책 준수"*.
  외부 이미지 0장, 외부 폰트 0개인 것이 이 조항에 대한 답입니다.
