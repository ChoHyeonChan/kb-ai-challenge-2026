# AGENTS.md — 이 프로젝트에서 일하는 모든 AI가 먼저 읽는다

> 제8회 KB Future Finance A.I. Challenge 출품작
> **마감 2026-08-03 16:00 · 제출 목표 8/02**
> 이 파일은 Codex·Claude 등 **모든 AI 에이전트의 공통 규약**이다. 사람용 안내는 `docs/TEAM_GUIDE.md`.

---

# 🔴 0. 보안이 최우선이다 — 다른 모든 것보다 앞선다

**API 키는 절대 유출되어서는 안 된다.** 기능 구현·일정·편의보다 우선한다.

| 금지 | 이유 |
|---|---|
| **키 값을 터미널·로그·에러 메시지에 출력하지 않는다** | 출력되는 순간 대화 기록·CI 로그에 남는다. 실제로 이 프로젝트에서 훅이 감지된 줄을 그대로 찍어 키가 노출된 사고가 있었다 |
| **키를 코드·주석·문서·커밋 메시지에 쓰지 않는다** | `os.getenv()` 로만 읽는다 |
| **`.env` 를 커밋하지 않는다** | `.gitignore` + pre-commit 훅으로 이중 차단 |
| **실제 키로 테스트하지 않는다** | 검증은 반드시 **가짜 키**(`sk-proj-0000...`)로 한다 |
| **키를 채팅·이슈·PR 본문에 붙여넣지 않는다** | 사람에게도 요구하지 않는다 |

**키가 한 번이라도 커밋되면 파일을 지워도 히스토리에 남는다. 유일한 대응은 즉시 폐기·재발급이다.**

### 설치된 방어선
1. `.gitignore` — `.env`, `.env.*`(단 `.env.example` 허용), `*.key`, `*.pem`, `credentials*`, `secrets*`, `data/.llm_cache/`
2. **pre-commit 훅** (`.githooks/pre-commit`) — `.env` 계열 차단 + 키 패턴 감지. **감지해도 값은 출력하지 않고 파일명만 알린다**
3. 코드는 환경변수로만 키를 읽는다
4. LLM 캐시에는 **응답 구조체만** 저장한다 (요청 헤더 미저장)

### clone 후 반드시 실행 (훅은 저장소에 딸려오지만 자동 활성화되지 않는다)
```bash
git config core.hooksPath .githooks
```

### 작업 중 지킬 것
- 커밋 전 `git status` 로 무엇이 올라가는지 눈으로 확인한다
- 리포는 **private** 을 유지한다
- 제출용 zip 을 만들 때 `.env`·캐시가 섞이지 않았는지 확인한다 (`docs/SUBMIT_CHECKLIST.md` 1번 항목)

---

## 1. 우리가 만드는 것 (30초 요약)

은행 앱은 "왜 안 되는지"를 말하지 않는다.
흩어진 **약관·이용조건 문서에서 조건을 자동 추출**해 **조건 트리**로 만들고,
사용자 상태와 대조해 **무엇에 막혔는지 + 근거 원문 + 다음 행동**을 판정한다.

```
문서 → [LLM 추출] → 조건트리 → [규칙엔진 판정] → 미충족 항목 + 근거 + 해결경로
                     ↑ 재사용 자산        ↑ 결정론적. LLM 개입 금지
```

시나리오 2종: ① 해외결제 활성화 ② 비대면 계좌개설

상세 기획은 `docs/기획안.md`. **작업 전에 해당 섹션을 읽는다.**

---

## 2. ★ 절대 규칙 (어기면 대회 실격이거나 배점 손실)

| # | 규칙 | 이유 |
|---|---|---|
| 1 | **판정 로직에 LLM을 호출하지 않는다** | 같은 입력에 같은 출력이 나와야 한다. 결정론성 100%를 측정해 제출한다 |
| 2 | **`.env`·API 키를 커밋하지 않는다** | 유출 시 복구 불가 |
| 3 | **8/2 제출 이후 git commit 금지** | *"참가접수기간 이후 수정·변경 이력 확인 시 심사 대상 제외"* — 공식 FAQ |
| 4 | **로그인 뒤 개인화 페이지를 수집하지 않는다** | 공시·안내 페이지만. 서약서 위반 = 실격 |
| 5 | **외부 이미지를 쓰지 않는다** | 제3자 저작권 침해 = 실격 조항. 시각요소는 CSS 자작 |
| 6 | **측정하지 않은 숫자를 문서·코드 주석에 쓰지 않는다** | "민원 20% 감소" 같은 추정치 금지 |
| 7 | **조건에 `evidence`가 없으면 트리에 넣지 않는다** | 근거 표시율 100%가 구조적 보장이어야 한다 |

---

## 3. 폴더 구조 — 기능별로 나눈다

> 심사위원이 열었을 때 **"어디에 뭐가 있는지"가 폴더 이름만으로 보여야 한다.**

```
app/
├─ README.md              ← 심사위원이 제일 먼저 보는 문서
├─ AGENTS.md              ← (이 파일의 사본 또는 심볼릭)
├─ requirements.txt
├─ .env.example           ← 키 이름만. 값 없음
├─ run.sh / run.bat       ← 한 줄 실행
│
├─ data/                  ── 데이터 (코드 아님)
│  ├─ sources/               수집 대상 URL 목록 (yaml)
│  ├─ raw/                   수집 원문 + 수집일 메타
│  ├─ chunks/                분할된 스니펫
│  ├─ trees/              ★ 조건 트리 = 핵심 산출물
│  ├─ review/                검수 기록 (승인·반려 사유)
│  └─ profiles/              가상 사용자 프로필
│
├─ src/
│  ├─ goals.py            ── 목표 정의 로더 (extract·resolve 공용)
│  ├─ collect/            ── 문서 수집·분할        [Claude]
│  │   ├─ fetch.py
│  │   └─ chunk.py
│  ├─ extract/            ── 조건 추출              [현찬]
│  │   ├─ vocabulary.py      허용 subject·op·category (추출·검증 공용)
│  │   ├─ examples.py        few-shot 예시
│  │   ├─ prompt.py          규칙 + 프롬프트 조립
│  │   ├─ candidates.py      조건 후보 청크 1차 선별
│  │   ├─ extractor.py       LLM 호출·캐시          ← LLM 은 여기까지
│  │   ├─ validate.py        검증 게이트 + 인용 자동 보정
│  │   ├─ agreement.py       문서 간 합의 측정 (대표 선정)
│  │   ├─ merge.py           조건 병합
│  │   ├─ review.py          검수 관문 (사람 승인)
│  │   └─ assemble.py        트리 파일 조립
│  ├─ judge/              ── 판정 엔진 (LLM 금지)  [Claude → 팀원 검증]
│  │   ├─ schema.py          C1/C2/C3 정의
│  │   ├─ predicate.py       op 평가기
│  │   └─ engine.py          judge()
│  ├─ resolve/            ── 목표 식별·설명 (LLM)  [현찬]
│  │   ├─ retriever.py       질의 → goal_id
│  │   └─ explain.py         미충족 사유 설명
│  └─ api/                ── HTTP 계층             [Claude]
│      └─ main.py
│
├─ web/                   ── 데모 화면             [Claude]
│  └─ index.html
│
├─ tests/                 ── 검증                  [팀원]
│  ├─ test_determinism.py    동일 입력 N회 일치율
│  ├─ test_edge.py           빈값·null·미지원 goal
│  └─ cases/                 테스트 케이스 데이터
│
├─ eval/                  ── 지표 측정
│  ├─ extraction_accuracy.py
│  ├─ labels/                수동 라벨 (자동 추출 전에 사람이 만든 정답지)
│  └─ results/               측정 결과 (제출물)
│
└─ docs/
   ├─ 기획안.md / .pdf
   ├─ EVIDENCE.md            근거 검증 대장
   ├─ REPRODUCE.md           클린설치 재현 절차
   └─ TEAM_GUIDE.md
```

### 폴더 규칙
- **`src/` 하위는 파이프라인 단계 = 폴더 하나.** 새 단계가 생기면 폴더를 만들지, 기존 폴더를 부풀리지 않는다.
- **데이터와 코드를 섞지 않는다.** 생성물은 전부 `data/` 또는 `eval/results/`.
- **담당자 표기를 지킨다.** 남의 폴더 파일을 고쳐야 하면 먼저 알린다.

---

## 4. 파일 분리 규칙 — 나중에 리팩토링하지 않기 위해

| 기준 | 한계 | 넘으면 |
|---|---|---|
| 파일 길이 | **200줄** | 책임 단위로 쪼갠다 |
| 함수 길이 | **40줄** | 헬퍼로 분리 |
| 파일당 책임 | **1개** | 이름이 `utils.py`·`common.py`가 되려 하면 잘못 가고 있는 신호 |
| import 방향 | **단방향** | `schema ← predicate ← engine ← api` 역방향·순환 금지 |

**처음부터 지킬 것**
- 새 기능을 기존 파일에 덧붙이기 전에 **"이게 이 파일의 책임인가?"** 를 먼저 묻는다.
- 프롬프트 문자열은 **`prompt.py`에만** 둔다. 로직 파일에 문자열로 박지 않는다.
- 상수·경로는 `config.py` 한 곳. 하드코딩 금지.

---

## 5. 인터페이스 계약 (C1/C2/C3) — 바꾸려면 합의 필요

세 사람이 병렬로 일하는 접점이다. **혼자 바꾸지 않는다.** 정의는 `src/judge/schema.py`가 단일 출처.

- **C1 조건 트리** — `extract/`가 생산 → `judge/`가 소비. `data/trees/*.json`
- **C2 사용자 프로필** — `data/profiles/*.json`. 모르는 값은 `null` (추측 금지)
- **C3 판정 결과** — `judge/`가 생산 → `api/` → `web/`. `unmet`/`met`/`unknown`/`low_confidence` 4분류

전체 스키마는 `docs/기획안.md` 7장 참조.

**설계 의도 (구현할 때 지킬 것)**
- `evidence` 필수 → 근거 없는 조건은 존재할 수 없다
- `evidence.confidence` → 추출 신뢰도. `low`면 화면에서 "검토 필요"
- `remedy.actionable_in_app` → **앱에서 못 고치는 조건**을 구분 (안심차단이 `false`)
- `unknown` → **모르는 건 모른다고 한다.** 전부 ✓/✕로 채우면 그게 거짓말

---

## 6. 코딩 규약

- Python 3.11+ / 타입 힌트 **필수** (특히 계약 경계)
- 스키마는 **pydantic v2**. 검증 실패 시 어느 필드가 왜 틀렸는지 나오게
- **LLM 호출은 `extract/`와 `resolve/`에서만.** `judge/`에서 `openai` import 하면 설계 위반
- LLM 호출 시 **`temperature=0`**, 응답은 **반드시 파일 캐싱** (비용 + 재현성)
- 랜덤·시각 의존 로직 금지. 필요하면 인자로 주입 (테스트 가능하게)
- 예외를 삼키지 않는다. `except: pass` 금지
- 주석은 **왜**를 쓴다. 무엇을은 코드가 말한다

### 커밋
```
[collect] KB 카드 FAQ 수집 스크립트 추가
[judge]   predicate 평가기 date_after 지원
[tests]   결정론성 테스트 10회 반복
[docs]    근거 대장 갱신
```
- 한 커밋 = 한 가지 변경
- **8/2 제출 이후 커밋 금지** (규칙 3)

---

## 7. 현재 담당

| | 현찬 (AI 코어) | 팀원 (검증·문서) | Claude (코드·표현) |
|---|---|---|---|
| 폴더 | `src/extract/` `src/resolve/` | `tests/` `data/profiles/` `docs/` | `src/collect/` `src/judge/` `src/api/` `web/` |
| 산출 | 조건트리 2종, 추출 프롬프트, RAG | 결정론성·엣지 테스트, 재현 절차, 기술설명서 | 수집·판정엔진·API·화면·문서 뼈대 |

---

## 8. AI 에이전트가 특히 조심할 것

1. **없는 API를 지어내지 않는다.** 라이브러리 사용법이 불확실하면 확인하고 쓴다.
2. **파일을 통째로 재작성하지 않는다.** 부분 수정으로 충분한지 먼저 본다.
3. **"됐습니다"라고 말하기 전에 실제로 돌려본다.** 테스트 없이 완료 보고 금지.
4. **기존 코드 스타일을 따른다.** 자기 취향으로 바꾸지 않는다.
5. **요청 범위를 넘지 않는다.** 옆 파일까지 "개선"하지 않는다.
6. 막히면 **3번 시도 후 멈추고 상황을 정리해 사람에게 보고**한다. 무한 루프 금지.

---

## 9. AI 사용 기록 (참가신청서 제출 항목)

참가신청서에 **AI 활용 신고표**가 있다. 작업하면서 아래를 `docs/AI_USAGE.md`에 계속 기록한다.

| 구분 | 도구 | 반영 정도 |
|---|---|---|
| 아이디어 발굴 / 정보수집 / 데이터분석 / **코드작성·디버깅** / 문서작성 / 이미지제작 / 발표자료제작 | 도구명 | 참고 · 일부반영 · 대부분활용 |

**숨기는 게 아니라 밝히는 구조다.** 쓴 대로 적으면 된다.

---

## 10. 막히면

- 기획·근거 → `docs/기획안.md`, `docs/EVIDENCE.md`
- 스키마 → `src/judge/schema.py` (단일 출처)
- 대회 요건 → `docs/대회요강.md`
- 그래도 모르면 **추측해서 진행하지 말고 물어본다.** 잘못된 가정 위에 쌓은 코드가 제일 비싸다.
